{
  "coreName": "PPSSPP",
  "author": "Henrik Rydgard and the PPSSPP team (chimera port: Sergio Martin)",
  "version": "1.0.0",
  "url": "https://github.com/ToolAssisted-run/chimera-core-ppsspp",
  "systemId": "PSP",
  "romFile": "rom",
  "deterministic": true,
  "memoryLayoutMiB": [
    128,
    16,
    64,
    64,
    1024
  ],
  "_memoryLayoutMiB_note": "sbrk, sealed, invis, plain, mmap. PPSSPP is a big machine: the 256MiB masked PSP address-space arena plus PPSSPP's own heap (softgpu bins, IR blocks, kernel objects) live in sbrk/mmap, and every green-thread stack is 8MiB of mmap arena.",
  "video": {
    "width": 480,
    "height": 272,
    "virtualWidth": 480,
    "virtualHeight": 272,
    "vsyncNumerator": 60000,
    "vsyncDenominator": 1001,
    "_vsync_note": "The PSP's refresh rate as PPSSPP models it: sceDisplay's vblank period is 1.001/60 of a second, so 60/1.001 = 59.94005994...Hz. The same NTSC ratio a PS2 and a Dreamcast run at. GetVsyncNumerator/Denominator in waterbox.cpp say the same thing and are what the engine actually reads; these are the answer for a frontend that asks the package instead.",
    "getBgra": "GetVideoBgra"
  },
  "audio": {
    "samplesPerFrame": 8192,
    "_samplesPerFrame_note": "capacity, not the count: the core exports GetAudioSampleCount and hands over the real number each frame (~735 at 44100Hz).",
    "channels": 2,
    "get": "GetAudio"
  },
  "input": {
    "name": "PSP Controller",
    "buttons": [
      "P1 Up",
      "P1 Down",
      "P1 Left",
      "P1 Right",
      "P1 Cross",
      "P1 Circle",
      "P1 Square",
      "P1 Triangle",
      "P1 Start",
      "P1 Select",
      "P1 L",
      "P1 R"
    ],
    "axes": [
      {
        "name": "P1 L-Stick X",
        "min": -128,
        "max": 127,
        "neutral": 0
      },
      {
        "name": "P1 L-Stick Y",
        "min": -128,
        "max": 127,
        "neutral": 0
      }
    ],
    "_axes_note": "Positive Y is up, the natural gamepad passthrough. The frontend feeds these through the SetAxis export before every frame."
  },
  "lag": {
    "inputWasRead": "InputWasRead"
  },
  "extensions": {
    ".iso": "PSP",
    ".cso": "PSP",
    ".pbp": "PSP",
    ".elf": "PSP",
    ".prx": "PSP"
  },
  "settings": [
    {
      "name": "cpuCore",
      "display": "CPU Core",
      "description": "How the PSP's MIPS CPU is executed. The x86 JIT is the default: much faster, and it runs inside the sandbox like everything else (generated code is ordinary guest memory, captured by savestates). The IR interpreter is the slower fallback. The two can differ in edge cases, so this shapes the machine and movies record it.",
      "type": "enum",
      "options": [
        "ir-interpreter",
        "interpreter",
        "jit"
      ],
      "default": "jit"
    },
    {
      "name": "pspModel",
      "display": "PSP Model",
      "description": "Which machine this is. The PSP-2000 (Slim) has 64MB of RAM, the PSP-1000 has 32MB; a handful of games use the extra memory. Shapes the machine, so changing it reboots the core and movies record it.",
      "type": "enum",
      "options": [
        "psp-1000",
        "psp-2000"
      ],
      "default": "psp-2000"
    },
    {
      "name": "language",
      "display": "System Language",
      "description": "The console's language system parameter. Games read it at boot to pick their language, so it shapes the machine.",
      "type": "enum",
      "options": [
        "japanese",
        "english",
        "french",
        "spanish",
        "german",
        "italian",
        "dutch",
        "portuguese",
        "russian",
        "korean",
        "chinese-traditional",
        "chinese-simplified"
      ],
      "default": "english"
    },
    {
      "name": "nickname",
      "display": "Nickname",
      "description": "The console owner's name system parameter. Games read it for save files and default player names.",
      "type": "string",
      "default": "Chimera"
    },
    {
      "name": "buttonPreference",
      "display": "Confirm Button",
      "description": "Which face button the system treats as confirm - Circle on Japanese consoles, Cross elsewhere. Games read it to lay out their menus.",
      "type": "enum",
      "options": [
        "cross",
        "circle"
      ],
      "default": "cross"
    },
    {
      "name": "rtcBase",
      "display": "Console Clock",
      "description": "The console's date and time at power-on, as YYYY-MM-DD HH:MM:SS. Games read the RTC for save timestamps, daily events and date-gated content; emulated time advances from this point with emulation, never with the real clock. An unparseable value falls back to the default.",
      "type": "string",
      "default": "2017-05-27 12:44:28"
    },
    {
      "name": "cpuClock",
      "display": "CPU Clock (MHz)",
      "description": "Lock the emulated CPU to this many MHz; 0 leaves the clock to the game (the hardware boots at 222 and games may raise it to 333). Underclocking and overclocking both change timing, so this shapes the machine.",
      "type": "int",
      "min": 0,
      "max": 333,
      "default": 0
    },
    {
      "name": "ioTiming",
      "display": "I/O Timing",
      "description": "How long emulated disc and memory-stick I/O takes: 'fast' completes quickly, 'simulated' models realistic access times, 'umd-slow' models a slow UMD drive. (Upstream's host-clock timing mode is nondeterministic and deliberately not offered.)",
      "type": "enum",
      "options": [
        "fast",
        "simulated",
        "umd-slow"
      ],
      "default": "fast"
    },
    {
      "name": "timeZone",
      "display": "Time Zone (minutes)",
      "description": "The console's UTC offset in minutes (-720..840), a system parameter games read alongside the clock.",
      "type": "int",
      "min": -720,
      "max": 840,
      "default": 0
    },
    {
      "name": "daylightSavings",
      "display": "Daylight Savings",
      "description": "The daylight-savings system parameter games can read.",
      "type": "bool",
      "default": false
    },
    {
      "name": "firmwareVersion",
      "display": "Firmware Version",
      "description": "The system software version games see (660 = 6.60). Some games check it at boot.",
      "type": "int",
      "min": 100,
      "max": 999,
      "default": 660
    },
    {
      "name": "macAddress",
      "display": "MAC Address",
      "description": "The console's WLAN MAC address as XX:XX:XX:XX:XX:XX. Games read it as a console identity and some seed randomness from it.",
      "type": "string",
      "default": "12:34:56:78:9A:BC"
    },
    {
      "name": "dateFormat",
      "display": "Date Format",
      "description": "The date-format system parameter games read for display.",
      "type": "enum",
      "options": [
        "yyyy-mm-dd",
        "mm-dd-yyyy",
        "dd-mm-yyyy"
      ],
      "default": "yyyy-mm-dd"
    },
    {
      "name": "timeFormat",
      "display": "Time Format",
      "description": "The time-format system parameter games read for display.",
      "type": "enum",
      "options": [
        "24h",
        "12h"
      ],
      "default": "24h"
    },
    {
      "name": "parentalLevel",
      "display": "Parental Level",
      "description": "The parental-control level system parameter (0..11). Some games gate content on it.",
      "type": "int",
      "min": 0,
      "max": 11,
      "default": 9
    },
    {
      "name": "funcReplacements",
      "display": "HLE Function Replacements",
      "description": "Replace known functions (memcpy and friends) with fast host implementations. On matches PPSSPP's default; off is closer to hardware timing.",
      "type": "bool",
      "default": true
    },
    {
      "name": "encryptSave",
      "display": "Encrypt Savedata",
      "description": "Kirk-encrypt savedata like a real console. Turning it off makes saves inspectable but no longer hardware-shaped.",
      "type": "bool",
      "default": true
    },
    {
      "name": "fontSource",
      "display": "System Fonts",
      "description": "bundled: the free replacement fonts the core ships - no firmware needed, different glyphs and metrics than a real console. sony: the console's own flash0 fonts - each becomes a firmware requirement on the wizard's firmware page, satisfied by your own dumps.",
      "type": "enum",
      "options": [
        "bundled",
        "sony"
      ],
      "default": "bundled"
    }
  ],
  "firmware": [
    {
      "id": "ltn0.pgf",
      "display": "System Font ltn0 (Latin sans-serif regular)",
      "description": "The console's sans-serif regular Latin font. Dump it from your own console's flash0:/font; a real dump is exactly 69108 bytes (the size a real console reports, recorded from hardware), so a file of that size and hash reads as ok. The listed hash is the file from Sony's final official firmware (6.61). Any readable file you point at is used, including your own replacement font - it is reported as custom or unrecognised rather than refused, and movies record its hash. Optional: without it the core uses the free replacement font it ships, which has different glyphs and metrics - a different machine, recorded in movies either way.",
      "size": 69108,
      "sha1": "BD0BE6D3C32C74F0CDE1B96E475F1936ECD0EC41",
      "label": "6.61 official",
      "name": "ltn0.pgf",
      "requiredWhen": {
        "setting": "fontSource",
        "is": "sony"
      }
    },
    {
      "id": "ltn1.pgf",
      "display": "System Font ltn1 (Latin serif regular)",
      "description": "The console's serif regular Latin font. Dump it from your own console's flash0:/font; a real dump is exactly 65124 bytes (the size a real console reports, recorded from hardware), so a file of that size and hash reads as ok. The listed hash is the file from Sony's final official firmware (6.61). Any readable file you point at is used, including your own replacement font - it is reported as custom or unrecognised rather than refused, and movies record its hash. Optional: without it the core uses the free replacement font it ships, which has different glyphs and metrics - a different machine, recorded in movies either way.",
      "size": 65124,
      "sha1": "1280929BB682D299453ECA425AAF86415F3C5192",
      "label": "6.61 official",
      "name": "ltn1.pgf",
      "requiredWhen": {
        "setting": "fontSource",
        "is": "sony"
      }
    },
    {
      "id": "ltn2.pgf",
      "display": "System Font ltn2 (Latin sans-serif italic)",
      "description": "The console's sans-serif italic Latin font. Dump it from your own console's flash0:/font; a real dump is exactly 72948 bytes (the size a real console reports, recorded from hardware), so a file of that size and hash reads as ok. The listed hash is the file from Sony's final official firmware (6.61). Any readable file you point at is used, including your own replacement font - it is reported as custom or unrecognised rather than refused, and movies record its hash. Optional: without it the core uses the free replacement font it ships, which has different glyphs and metrics - a different machine, recorded in movies either way.",
      "size": 72948,
      "sha1": "10D4C7445DE6A7AF273A2CDC0393FEF9F9A9BDC2",
      "label": "6.61 official",
      "name": "ltn2.pgf",
      "requiredWhen": {
        "setting": "fontSource",
        "is": "sony"
      }
    },
    {
      "id": "ltn3.pgf",
      "display": "System Font ltn3 (Latin serif italic)",
      "description": "The console's serif italic Latin font. Dump it from your own console's flash0:/font; a real dump is exactly 67700 bytes (the size a real console reports, recorded from hardware), so a file of that size and hash reads as ok. The listed hash is the file from Sony's final official firmware (6.61). Any readable file you point at is used, including your own replacement font - it is reported as custom or unrecognised rather than refused, and movies record its hash. Optional: without it the core uses the free replacement font it ships, which has different glyphs and metrics - a different machine, recorded in movies either way.",
      "size": 67700,
      "sha1": "CF355AEC8E014752A198196DE835262326B6B7F4",
      "label": "6.61 official",
      "name": "ltn3.pgf",
      "requiredWhen": {
        "setting": "fontSource",
        "is": "sony"
      }
    },
    {
      "id": "ltn4.pgf",
      "display": "System Font ltn4 (Latin sans-serif bold)",
      "description": "The console's sans-serif bold Latin font. Dump it from your own console's flash0:/font; a real dump is exactly 72828 bytes (the size a real console reports, recorded from hardware), so a file of that size and hash reads as ok. The listed hash is the file from Sony's final official firmware (6.61). Any readable file you point at is used, including your own replacement font - it is reported as custom or unrecognised rather than refused, and movies record its hash. Optional: without it the core uses the free replacement font it ships, which has different glyphs and metrics - a different machine, recorded in movies either way.",
      "size": 72828,
      "sha1": "ECFC5E261E4218C2C4559BD20BE39EC5A52A9518",
      "label": "6.61 official",
      "name": "ltn4.pgf",
      "requiredWhen": {
        "setting": "fontSource",
        "is": "sony"
      }
    },
    {
      "id": "ltn5.pgf",
      "display": "System Font ltn5 (Latin serif bold)",
      "description": "The console's serif bold Latin font. Dump it from your own console's flash0:/font; a real dump is exactly 68220 bytes (the size a real console reports, recorded from hardware), so a file of that size and hash reads as ok. The listed hash is the file from Sony's final official firmware (6.61). Any readable file you point at is used, including your own replacement font - it is reported as custom or unrecognised rather than refused, and movies record its hash. Optional: without it the core uses the free replacement font it ships, which has different glyphs and metrics - a different machine, recorded in movies either way.",
      "size": 68220,
      "sha1": "37122C62E9B158EE757833C76F5FA48F902D200E",
      "label": "6.61 official",
      "name": "ltn5.pgf",
      "requiredWhen": {
        "setting": "fontSource",
        "is": "sony"
      }
    },
    {
      "id": "ltn6.pgf",
      "display": "System Font ltn6 (Latin sans-serif bold italic)",
      "description": "The console's sans-serif bold italic Latin font. Dump it from your own console's flash0:/font; a real dump is exactly 77032 bytes (the size a real console reports, recorded from hardware), so a file of that size and hash reads as ok. The listed hash is the file from Sony's final official firmware (6.61). Any readable file you point at is used, including your own replacement font - it is reported as custom or unrecognised rather than refused, and movies record its hash. Optional: without it the core uses the free replacement font it ships, which has different glyphs and metrics - a different machine, recorded in movies either way.",
      "size": 77032,
      "sha1": "67E34F24DBCB92F012A79D5741A9850614988A2B",
      "label": "6.61 official",
      "name": "ltn6.pgf",
      "requiredWhen": {
        "setting": "fontSource",
        "is": "sony"
      }
    },
    {
      "id": "ltn7.pgf",
      "display": "System Font ltn7 (Latin serif bold italic)",
      "description": "The console's serif bold italic Latin font. Dump it from your own console's flash0:/font; a real dump is exactly 71144 bytes (the size a real console reports, recorded from hardware), so a file of that size and hash reads as ok. The listed hash is the file from Sony's final official firmware (6.61). Any readable file you point at is used, including your own replacement font - it is reported as custom or unrecognised rather than refused, and movies record its hash. Optional: without it the core uses the free replacement font it ships, which has different glyphs and metrics - a different machine, recorded in movies either way.",
      "size": 71144,
      "sha1": "EF74B4B915CEEA2CFD688E1B53FB7D091199B3DB",
      "label": "6.61 official",
      "name": "ltn7.pgf",
      "requiredWhen": {
        "setting": "fontSource",
        "is": "sony"
      }
    },
    {
      "id": "ltn8.pgf",
      "display": "System Font ltn8 (Latin small sans-serif regular)",
      "description": "The console's small sans-serif regular Latin font. Dump it from your own console's flash0:/font; a real dump is exactly 41000 bytes (the size a real console reports, recorded from hardware), so a file of that size and hash reads as ok. The listed hash is the file from Sony's final official firmware (6.61). Any readable file you point at is used, including your own replacement font - it is reported as custom or unrecognised rather than refused, and movies record its hash. Optional: without it the core uses the free replacement font it ships, which has different glyphs and metrics - a different machine, recorded in movies either way.",
      "size": 41000,
      "sha1": "CAF71C8BED923749EA3B8BB96BC0A58C4C105298",
      "label": "6.61 official",
      "name": "ltn8.pgf",
      "requiredWhen": {
        "setting": "fontSource",
        "is": "sony"
      }
    },
    {
      "id": "ltn9.pgf",
      "display": "System Font ltn9 (Latin small serif regular)",
      "description": "The console's small serif regular Latin font. Dump it from your own console's flash0:/font; a real dump is exactly 40164 bytes (the size a real console reports, recorded from hardware), so a file of that size and hash reads as ok. The listed hash is the file from Sony's final official firmware (6.61). Any readable file you point at is used, including your own replacement font - it is reported as custom or unrecognised rather than refused, and movies record its hash. Optional: without it the core uses the free replacement font it ships, which has different glyphs and metrics - a different machine, recorded in movies either way.",
      "size": 40164,
      "sha1": "7B48D6BACA15B4D1C1C525AEA9486F50C37D5237",
      "label": "6.61 official",
      "name": "ltn9.pgf",
      "requiredWhen": {
        "setting": "fontSource",
        "is": "sony"
      }
    },
    {
      "id": "ltn10.pgf",
      "display": "System Font ltn10 (Latin small sans-serif italic)",
      "description": "The console's small sans-serif italic Latin font. Dump it from your own console's flash0:/font; a real dump is exactly 42692 bytes (the size a real console reports, recorded from hardware), so a file of that size and hash reads as ok. The listed hash is the file from Sony's final official firmware (6.61). Any readable file you point at is used, including your own replacement font - it is reported as custom or unrecognised rather than refused, and movies record its hash. Optional: without it the core uses the free replacement font it ships, which has different glyphs and metrics - a different machine, recorded in movies either way.",
      "size": 42692,
      "sha1": "EB44C801F61B585A3E5D027D505C179B4CC2E594",
      "label": "6.61 official",
      "name": "ltn10.pgf",
      "requiredWhen": {
        "setting": "fontSource",
        "is": "sony"
      }
    },
    {
      "id": "ltn11.pgf",
      "display": "System Font ltn11 (Latin small serif italic)",
      "description": "The console's small serif italic Latin font. Dump it from your own console's flash0:/font; a real dump is exactly 41488 bytes (the size a real console reports, recorded from hardware), so a file of that size and hash reads as ok. The listed hash is the file from Sony's final official firmware (6.61). Any readable file you point at is used, including your own replacement font - it is reported as custom or unrecognised rather than refused, and movies record its hash. Optional: without it the core uses the free replacement font it ships, which has different glyphs and metrics - a different machine, recorded in movies either way.",
      "size": 41488,
      "sha1": "DF774719009CE1D4A3EA8C4E496F7454BE07EEB4",
      "label": "6.61 official",
      "name": "ltn11.pgf",
      "requiredWhen": {
        "setting": "fontSource",
        "is": "sony"
      }
    },
    {
      "id": "ltn12.pgf",
      "display": "System Font ltn12 (Latin small sans-serif bold)",
      "description": "The console's small sans-serif bold Latin font. Dump it from your own console's flash0:/font; a real dump is exactly 43136 bytes (the size a real console reports, recorded from hardware), so a file of that size and hash reads as ok. The listed hash is the file from Sony's final official firmware (6.61). Any readable file you point at is used, including your own replacement font - it is reported as custom or unrecognised rather than refused, and movies record its hash. Optional: without it the core uses the free replacement font it ships, which has different glyphs and metrics - a different machine, recorded in movies either way.",
      "size": 43136,
      "sha1": "D7A36607B2607CB83D8195FEA71B0F1FFC07F5ED",
      "label": "6.61 official",
      "name": "ltn12.pgf",
      "requiredWhen": {
        "setting": "fontSource",
        "is": "sony"
      }
    },
    {
      "id": "ltn13.pgf",
      "display": "System Font ltn13 (Latin small serif bold)",
      "description": "The console's small serif bold Latin font. Dump it from your own console's flash0:/font; a real dump is exactly 41772 bytes (the size a real console reports, recorded from hardware), so a file of that size and hash reads as ok. The listed hash is the file from Sony's final official firmware (6.61). Any readable file you point at is used, including your own replacement font - it is reported as custom or unrecognised rather than refused, and movies record its hash. Optional: without it the core uses the free replacement font it ships, which has different glyphs and metrics - a different machine, recorded in movies either way.",
      "size": 41772,
      "sha1": "0C1ACD5A545D1DCB6E9AD2967E7FD5FE54864FBB",
      "label": "6.61 official",
      "name": "ltn13.pgf",
      "requiredWhen": {
        "setting": "fontSource",
        "is": "sony"
      }
    },
    {
      "id": "ltn14.pgf",
      "display": "System Font ltn14 (Latin small sans-serif bold italic)",
      "description": "The console's small sans-serif bold italic Latin font. Dump it from your own console's flash0:/font; a real dump is exactly 45184 bytes (the size a real console reports, recorded from hardware), so a file of that size and hash reads as ok. The listed hash is the file from Sony's final official firmware (6.61). Any readable file you point at is used, including your own replacement font - it is reported as custom or unrecognised rather than refused, and movies record its hash. Optional: without it the core uses the free replacement font it ships, which has different glyphs and metrics - a different machine, recorded in movies either way.",
      "size": 45184,
      "sha1": "0CD842780701A7B7F196F04FF1221C953510A6C4",
      "label": "6.61 official",
      "name": "ltn14.pgf",
      "requiredWhen": {
        "setting": "fontSource",
        "is": "sony"
      }
    },
    {
      "id": "ltn15.pgf",
      "display": "System Font ltn15 (Latin small serif bold italic)",
      "description": "The console's small serif bold italic Latin font. Dump it from your own console's flash0:/font; a real dump is exactly 43044 bytes (the size a real console reports, recorded from hardware), so a file of that size and hash reads as ok. The listed hash is the file from Sony's final official firmware (6.61). Any readable file you point at is used, including your own replacement font - it is reported as custom or unrecognised rather than refused, and movies record its hash. Optional: without it the core uses the free replacement font it ships, which has different glyphs and metrics - a different machine, recorded in movies either way.",
      "size": 43044,
      "sha1": "A69933EC3E93ACEF7800D5F75273924C205F02A6",
      "label": "6.61 official",
      "name": "ltn15.pgf",
      "requiredWhen": {
        "setting": "fontSource",
        "is": "sony"
      }
    },
    {
      "id": "jpn0.pgf",
      "display": "System Font jpn0 (Japanese)",
      "description": "The console's Japanese font. Dump it from your own console's flash0:/font; a real dump is exactly 1581700 bytes (the size a real console reports, recorded from hardware), so a file of that size and hash reads as ok. The listed hash is the file from Sony's final official firmware (6.61). Any readable file you point at is used, including your own replacement font - it is reported as custom or unrecognised rather than refused, and movies record its hash. Optional: without it the core uses the free replacement font it ships, which has different glyphs and metrics - a different machine, recorded in movies either way.",
      "size": 1581700,
      "sha1": "9EE2931E477CF10BC0B764714E9F0A21AAAED3B4",
      "label": "6.61 official",
      "name": "jpn0.pgf",
      "requiredWhen": {
        "setting": "fontSource",
        "is": "sony"
      }
    },
    {
      "id": "kr0.pgf",
      "display": "System Font kr0 (Korean)",
      "description": "The console's Korean font. Dump it from your own console's flash0:/font; a real dump is exactly 394192 bytes (the size a real console reports, recorded from hardware), so a file of that size and hash reads as ok. The listed hash is the file from Sony's final official firmware (6.61). Any readable file you point at is used, including your own replacement font - it is reported as custom or unrecognised rather than refused, and movies record its hash. Optional: without it the core uses the free replacement font it ships, which has different glyphs and metrics - a different machine, recorded in movies either way.",
      "size": 394192,
      "sha1": "EB0A5001A54BDAB092DB1B015544A42BBE817515",
      "label": "6.61 official",
      "name": "kr0.pgf",
      "requiredWhen": {
        "setting": "fontSource",
        "is": "sony"
      }
    },
    {
      "id": "zh_gb.pgf",
      "display": "System Font zh_gb (Chinese)",
      "description": "The Chinese font certain games install rather than any console shipping it; sceFont reads it from flash0 when present. Dump it from your own console's flash0:/font. Optional: without it the core uses the free replacement font it ships, which has different glyphs and metrics - a different machine, recorded in movies either way.",
      "size": 0,
      "name": "zh_gb.pgf",
      "requiredWhen": {
        "setting": "fontSource",
        "is": "sony"
      }
    }
  ]
}
